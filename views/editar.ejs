<!DOCTYPE html>
<html lang="en">
<%- include('includes/head', {title:"Editar Pizza"}) %>
<body>
    <%- include('includes/header') %>
    <main class="container d-flex justify-content-center align-items-center vh-75 flex-column">        
        <form action="/cardapio/editar?_method=PUT" method="POST" enctype="multipart/form-data" class="form-cadastro p-3">
            <h1>Editar Pizza</h1>
            <input type="number" value="<%= posicao %>" name="posicao" hidden readonly>
            <div class="form-group">
                <label for="nomePizza">Nome da Pizza</label>
                <input type="text" class="form-control" name="nomePizza" id="nomePizza" value="<%= pizza.nome %>">                
            </div>
            <div class="form-group">
                <label for="precoPizza">Preço da Pizza</label>
                <input type="number" class="form-control" name="precoPizza" id="precoPizza" value="<%= pizza.preco %>">
            </div>
            <div class="form-group">
                <img class="img-edit ml-5" src="/img/<%= pizza.img %>" alt="<%= pizza.nome %>">
                <label for="fotoPizza">Foto da Pizza</label>
                <input type="file" class="form-control" name="fotoPizza" id="fotoPizza">
            </div>            
            <button type="submit" class="btn btn-success">Editar</button>
        </form>
        <% if(typeof(msg) != 'undefined') { %>
            <div class="d-flex justify-content-end">
                <div class="alert alert-success">
                    <%= msg %>
                </div>
            </div>
        <% } %> 
    </main>
</body>
</html>